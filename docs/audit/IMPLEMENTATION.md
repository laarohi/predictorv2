# Implementation Map

Every confirmed finding → status → commit. **Status legend:** ✅ Fixed · 📝 Documented (no code change — judgment call, recorded with rationale) · ⏭️ Deferred (intentionally not done pre-launch).

Each fix is an isolated commit so you can cherry-pick into `main`. Commit hashes are filled in as work lands.

_This table is populated incrementally during the fix phase._
